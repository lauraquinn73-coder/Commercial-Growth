COMMERCIAL GROWTH ARTICLE PATCH

Upload the resources folder to the ROOT of the GitHub repository so the live paths become /resources/<article>.html. Also upload sitemap.xml, robots.txt and feed.xml to the repo root, replacing existing copies if prompted.
